# Cerberus-Lint
A high-performance, memory-efficient DevSecOps utility that streams server logs, sanitizes inputs, and extracts brute-force indicators of compromise (IoCs) into structured JSON payloads.
